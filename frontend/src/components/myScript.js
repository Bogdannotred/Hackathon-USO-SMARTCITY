// myScript.js

function detectImage(image) {
    // Logică pentru a detecta imaginea cu Roboflow
    console.log("Detecting image:", image);
  }
  
  module.exports = { detectImage }; // Exportă funcția pentru a o folosi în alt fișier
  